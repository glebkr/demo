app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/demodb
    username: postgres
    password: password
    pool-size: 30
spring:
  kafka:
    consumer:
      bootstrap-server: localhost:9092
      group-id: group
    producer:
      bootstrap-server: localhost:9092
  cache:
    type: redis
    host: localhost
    port: 6379
    redis:
      time-to-live: 60000